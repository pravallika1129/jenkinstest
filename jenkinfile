pipeline{
	agent { label 'maven' }
	stages{
		stage('clone git repo'){
			steps{
				echo 'first stage'
			}
		}
		stage('maven build - package '){
			steps{
				echo 'second stage'
			}
		}
		stage('maven build - package '){
			steps{
				echo 'third stage'
			}
		}
	}
}
